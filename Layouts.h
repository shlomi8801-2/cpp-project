#pragma once
#include "Screen.h"

namespace levels {
	void changeLayout(int levelNum, Screen* screen);
    void level1(Screen* screen);
    

    // Helper to load by number
    void changeLayout(int levelNum, Screen* screen);
}